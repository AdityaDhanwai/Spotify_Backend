using Microsoft.AspNetCore.Identity;

namespace Spotify_Backend_Assignment.Models
{
    public class ApplicationUser : IdentityUser
    {
        // We can extend with more fields later like Name, DOB, etc.
    }
}
